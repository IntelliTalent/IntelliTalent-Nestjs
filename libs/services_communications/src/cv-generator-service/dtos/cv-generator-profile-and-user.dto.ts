import { Profile } from "@app/shared";

export class CVGeneratorProfileAndUserData extends Profile {
  fullName: string;
  email: string;
  address: string;
  phoneNumber: string;
  city: string;
  country: string;
}
