export enum StageType {
  created = 'created',
  matched = 'matched',
  applied = 'applied',
  quiz = 'quiz',
  interview = 'interview',
  failed = 'failed',
  candidate = 'candidate',
  selected = 'selected'
}